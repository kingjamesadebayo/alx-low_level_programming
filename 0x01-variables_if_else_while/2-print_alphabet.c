#include <stdio>

/**
 * main - A program that prints the alphabeth in lowercase 
 *
 * Return; Always 0
 */
int main(void)
{
	char letter;

	for(lettr = 'a'; letter <= 'Z'; letter++)
	putchar(letter);
	putchar('\n');
	return (0)
}
